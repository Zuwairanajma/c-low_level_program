#include <stdio.h>
#include <stdlib.h>
/**
prints numbers from one to hundred followed by a new line
*/
int main()
{
   int num;
   for(num = 1; num <= 100; num++)
   {
       printf("%d \n", num);
   }
    return 0;
}
